# Online-Order-FS

## Installation
```sh
client
yarn install
yarn serve

server
yarn install
yarn dev

migrate & seed
npx sequelize-cli db:migrate
npx sequelize-cli db:seed:all

buka localhost:8080 pada browser
```
